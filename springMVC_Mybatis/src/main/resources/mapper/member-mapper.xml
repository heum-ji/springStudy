<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">
	
	<select id="selectOneMember" parameterType="m" resultType="m">
		select
			member_id as memberId,
			member_pw as memberPw,
			member_name as memberName,
			phone,
			address,
			gender
		from member where member_id = #{memberId} and member_pw = #{memberPw}
	</select>

	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="m">
		insert into member
		values(#{memberId}, #{memberPw}, #{memberName}, #{phone}, #{address},
		#{gender})
	</insert>

	<!-- 회원탈퇴 -->
	<delete id="deleteMember" parameterType="string">
		delete from member where
		member_id = #{memberId}
	</delete>

	<!-- 아이디 찾기 -->
	<select id="searchId" parameterType="m" resultType="string">
		select
		member_id as memberId
		from member where member_name = #{memberName} and phone = #{phone}
	</select>
</mapper>
